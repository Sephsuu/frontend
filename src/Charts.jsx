import * as echarts from 'echarts';

export default function Charts() {
    const bar_graph = echarts.init.getElementById("bar");
    return(
        <section className="bar">
        
        </section>
    );
}